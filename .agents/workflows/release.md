# CONVERA Release & Checkpoint Workflow

1. Ensure working directory is clean: `git status`.
2. Run full verification suite (`/verify`).
3. Verify documentation reflects current codebase reality.
4. Commit with structured semantic message: `git commit -m "feat/fix: description"`.
5. Tag release checkpoint: `git tag vX.Y.Z`.
